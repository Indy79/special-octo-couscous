import React from 'react';
import { Link } from 'react-router-dom';

const Beers = () => (
    <div>
        Beers page lazy loaded
        <Link to="/">Home</Link>
    </div>
);

export default Beers;
